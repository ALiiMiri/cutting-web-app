# مرجع تسک یراق انبار، کارخانه و نصاب

این سند مرجع ماندگار ساختار «کد درب»، یراق دفتر و انبار، اقلام کارخانه و
محل‌های نصب نصاب است. مبنای این بررسی نسخه `bee5434` روی شاخه
`ui/typography-foundation` در تاریخ ۲۰۲۶-۰۸-۰۳ است.

## مدل کسب‌وکار نهایی

- هر ردیف جدول `doors` یک **کد درب با مشخصات فنی مشترک** است، نه یک محل نصب.
- هر کد درب می‌تواند چند محل نصب در جدول `door_installation_locations` داشته
  باشد و هر محل تعداد مستقل خود را دارد.
- مقدار `doors.quantity` همیشه جمع تعداد محل‌های نصب همان کد است.
- فیلد `doors.location` خلاصه‌ای از نام محل‌ها را برای سازگاری با بخش‌های قدیمی
  نگه می‌دارد؛ منبع اصلی محل و تعداد، جدول `door_installation_locations` است.
- ابعاد، جهت، پروفیل، رنگ پروفیل، چارچوب، یراق و حالت براکت بین تمام محل‌های یک
  کد مشترک‌اند. تفاوت در هرکدام از این موارد به معنی نیاز به کد درب جداگانه است.
- کد درب داخل هر سفارش بدون توجه به بزرگی و کوچکی حروف یکتا است.

## قواعد اعتبارسنجی

- کد درب الزامی و حداکثر ۴۰ نویسه است.
- هر کد حداقل یک و حداکثر ۱۰۰ محل نصب دارد.
- نام محل نصب الزامی و حداکثر ۱۶۰ نویسه است.
- تعداد هر محل باید عدد صحیح و بزرگ‌تر از صفر باشد.
- عرض و ارتفاع بزرگ‌تر از صفر، جهت فقط `راست` یا `چپ` و چارچوب فقط
  `دو طرفه` یا `سه طرفه` است.
- نوع و رنگ پروفیل الزامی‌اند.
- مدل دستگیره اختیاری است؛ اگر خالی باشد در خلاصه و خروجی یراق نمایش داده
  نمی‌شود و اگر مقدار داشته باشد در خروجی می‌آید.

اعتبارسنجی کد و محل‌ها در `_normalize_door_code_locations` و اعتبارسنجی یراق در
`normalize_door_hardware` انجام می‌شود. ذخیره یا ویرایش یک کد، محل‌ها، یراق و
مشخصات فنی در یک تراکنش انجام می‌شود.

## رابط اصلی سفارش

فرم اصلی در `templates/project_details.html` سه مرحله دارد:

1. کد، ابعاد، جهت، نوع و رنگ پروفیل و نوع چارچوب
2. مشخصات کامل یراق و انتخاب براکت خودکار/گوشتی
3. محل‌های نصب و تعداد هر محل همراه با نمایش جمع کل

مسیرهای اصلی ذخیره:

- `POST /project/<project_id>/quick_add_door`
- `POST /project/<project_id>/update_door/<door_id>`

توابع دیتابیس اصلی:

- `add_door_code_with_hardware_db`
- `update_door_code_with_hardware_db`
- `get_next_door_code_db`
- `get_doors_for_project_db`

## خروجی‌ها

### کارخانه

- خروجی بر اساس کد درب و تعداد کل آن کد است.
- محل‌های نصب در این خروجی نمایش داده نمی‌شوند.
- لاستیک با همان فرمول قیمت محاسبه و به متر کامل رو به بالا گرد می‌شود.
- تعداد براکت برای هر درب برابر دو طرف ضرب‌در بازه‌های ۶۰ سانتی‌متری ارتفاع است
  و سپس در تعداد کل کد ضرب می‌شود.
- در حالت عادی نام براکت از نوع پروفیل می‌آید؛ براکت گوشتی جایگزین براکت وابسته
  به پروفیل است.

مسیرها:

- `/project/<project_id>/factory-requirements`
- `/project/<project_id>/factory-requirements/excel`

### نصاب

- هر محل نصب به یک ردیف مستقل تبدیل می‌شود.
- هر ردیف شامل کد درب، محل نصب، تعداد همان محل، ابعاد و جهت است.

مسیرها:

- `/project/<project_id>/installer`
- `/project/<project_id>/installer/excel`

### دفتر و انبار

- خلاصه خرید تعداد اقلام را بر اساس تعداد کل هر کد جمع می‌کند.
- جزئیات به تفکیک کد درب نمایش داده می‌شود.
- مدل اختیاری دستگیره فقط در صورت وجود وارد عنوان قلم می‌شود.

مسیرها:

- `/project/<project_id>/hardware`
- `/project/<project_id>/hardware/excel`

## مهاجرت و سازگاری داده

مهاجرت `migrations/031_door_codes_and_locations.py`:

- ستون `doors.door_code` را اضافه می‌کند.
- جدول `door_installation_locations` و ایندکس مرتب‌سازی محل‌ها را ایجاد می‌کند.
- ایندکس یکتای کد درب در محدوده هر سفارش را ایجاد می‌کند.
- برای هر درب قدیمی یک کد `D-01`، `D-02` و مانند آن می‌سازد.
- محل و تعداد قدیمی را بدون کاهش داده به اولین محل نصب همان کد منتقل می‌کند.

جدول محل‌های نصب در `safe_upgrade.py` جزو جداول محافظت‌شده است؛ ارتقا باید پیش
از مهاجرت بکاپ بگیرد، کاهش تعداد رکوردهای محافظت‌شده را رد کند و کنترل‌های
`integrity_check` و `foreign_key_check` را بگذراند.

وضعیت انتشار ثبت‌شده در زمان تحویل:

- نسخه: `bee5434`
- مهاجرت: `031_door_codes_and_locations`
- ۹۰ ردیف قدیمی مهاجرت شدند.
- ۱۸۷ آزمون موفق بودند.
- بکاپ پیش از انتشار: `backup_14050512_140335_645f526b.db`
- دیتابیس بدون کد خالی، اختلاف تعداد یا خطای کلید خارجی گزارش شد.

بازبینی فقط‌خواندنی دیتابیس اصلی در ۲۰۲۶-۰۸-۰۳ نیز نتیجه زیر را تأیید کرد:

- ۹۰ کد درب و ۹۰ محل نصب
- صفر کد خالی و صفر کد تکراری داخل سفارش
- صفر کد بدون محل نصب و صفر اختلاف بین جمع محل‌ها و `doors.quantity`
- نتیجه `integrity_check` برابر `ok` و تعداد خطاهای کلید خارجی صفر

## منابع کد و آزمون

- `migrations/031_door_codes_and_locations.py`: ساختار و انتقال داده
- `database.py`: خواندن و ذخیره اتمیک کدها، محل‌ها و یراق
- `cutting_web_app.py`: اعتبارسنجی، مسیرها و Excelها
- `templates/project_details.html`: فرم اصلی سه‌مرحله‌ای
- `templates/factory_requirements.html`: خروجی کارخانه
- `templates/installer_report.html`: خروجی نصاب
- `templates/hardware_report.html`: خروجی دفتر و انبار
- `factory_requirements.py`: فرمول لاستیک و براکت
- `hardware_calculator.py`: تجمیع یراق دفتر و انبار
- `tests/test_door_codes.py`: مهاجرت، جمع تعداد، یکتایی کد و خروجی نصاب
- `tests/test_door_hardware.py`: قواعد یراق و مدل اختیاری دستگیره
- `tests/test_factory_requirements.py`: فرمول‌ها و براکت وابسته به پروفیل
- `tests/test_migration_safety.py`: ایمنی و یکپارچگی مهاجرت‌ها

## وضعیت واقعی بخش‌های سازگاری و پیگیری

موارد زیر در نسخه `bee5434` هنوز به‌طور کامل یکپارچه نشده‌اند و نباید در ادامه
کار به‌اشتباه «حذف‌شده» یا «تمام‌شده» فرض شوند:

1. **ویرایش گروهی:** قابلیت انتخاب چند ردیف و ویرایش پروفیل، رنگ، چارچوب، یراق و
   براکت حفظ شده، اما هنوز از `project_treeview.html` آغاز می‌شود و صفحه جداگانه
   `batch_edit.html` را باز می‌کند. محل‌های نصب و تعداد هر محل در ویرایش گروهی
   نیستند که مطابق تصمیم کسب‌وکار است.
2. **نمای پیشرفته:** جدول قدیمی همچنان با عنوان و لینک «جدول کامل» موجود است و
   هنوز در رابط به «نمای پیشرفته دستور ساخت» تغییر نام نداده است.
3. **مسیر قدیمی افزودن:** مسیرهای GET/POST مربوط به
   `/project/<project_id>/add_door` و مسیر `finish_doors` همچنان در
   `cutting_web_app.py` فعال‌اند و `templates/add_door.html` نیز وجود دارد. مسیر
   اصلی رابط به فرم سه‌مرحله‌ای اشاره می‌کند، اما مسیر قدیمی در سطح کد غیرفعال
   نشده است.
4. **تکرار سریع یراق:** قابلیت «استفاده از همین یراق برای درب‌های بعدی»، دکمه
   «ثبت درب با همین یراق» و انتخاب «فقط همین درب/از این درب به بعد» در نسخه
   `a7735ea` وجود داشت، اما در بازنویسی سه‌مرحله‌ای `bee5434` در فرم جدید حاضر
   نیست. اگر این نیاز همچنان معتبر است باید برای ساختار کد درب و مرحله محل‌ها
   دوباره طراحی و پیاده‌سازی شود.

## چک‌لیست تغییرات آینده

- محل‌های نصب را فقط از مرحله سوم همان کد تغییر دهید؛ آن‌ها را وارد ویرایش
  گروهی نکنید.
- هیچ خروجی کارخانه‌ای نباید محل نصب را نمایش دهد.
- خروجی نصاب باید تعداد را در سطح هر محل نگه دارد.
- تمام محاسبات کارخانه و انبار باید از `doors.quantity` که جمع محل‌هاست استفاده
  کنند.
- ایجاد و ویرایش کد باید محل‌ها، جمع تعداد، یراق و مشخصات فنی را اتمیک نگه دارد.
- مدل خالی دستگیره نباید اجباری یا در خروجی قابل مشاهده شود.
- قبل از حذف مسیرهای قدیمی، تمام لینک‌ها، sessionهای `pending_doors_*` و توابع
  سازگاری وابسته بررسی و سپس با ریدایرکت یا مهاجرت کنترل‌شده بازنشسته شوند.
