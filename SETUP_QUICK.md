# راه‌اندازی سریع سیستم لاگین

## قدم 1: نصب Flask-Login
```bash
pip install Flask-Login==0.6.3
```

یا از requirements.txt:
```bash
pip install -r requirements.txt
```

## قدم 2: ایجاد ادمین اولیه
```bash
python create_admin.py
```

وقتی اجرا کنید، از شما خواهد پرسید:
- نام کاربری (پیش‌فرض: `admin`)
- رمز عبور (حداقل 6 کاراکتر)

## قدم 3: اجرای برنامه
```bash
python cutting_web_app.py
```

## قدم 4: ورود به سیستم
1. مرورگر را باز کنید و به `http://localhost:5000` بروید
2. با نام کاربری و رمز ادمین وارد شوید
3. برای مدیریت کاربران به `/admin/users` بروید

## تمام! 🎉

---

## نقش‌ها

| نقش | دسترسی‌ها |
|-----|----------|
| **admin** | همه چیز + مدیریت کاربران + بکاپ + حذف پروژه + تنظیمات قیمت |
| **staff** | ایجاد/ویرایش پروژه‌ها، درب‌ها، انبار، محاسبات |
| **read_only** | فقط مشاهده (بدون ایجاد/ویرایش/حذف) |

---

## نکات مهم

- ✅ **رمز موقت**: وقتی ادمین کاربر جدید می‌سازد، کاربر در اولین ورود باید رمز را تغییر دهد
- ✅ **قفل خودکار**: بعد از 5 تلاش ناموفق، حساب 15 دقیقه قفل می‌شود
- ✅ **SECRET_KEY**: در production حتماً یک SECRET_KEY قوی در `.env` بگذارید:
  ```
  SECRET_KEY=your-very-long-random-key-here
  ```

---

## عیب‌یابی سریع

**مشکل: "flask_login not found"**  
→ `pip install Flask-Login`

**مشکل: "users table not found"**  
→ Migration خودکار اجرا می‌شود، اما اگر مشکل دارد: دیتابیس را پاک کنید و دوباره اجرا کنید

**مشکل: "نمی‌توانم وارد شوم"**  
→ ابتدا `python create_admin.py` را اجرا کنید

---

برای جزئیات بیشتر: `AUTH_SYSTEM_GUIDE.md`
