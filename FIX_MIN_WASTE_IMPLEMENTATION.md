# 🔧 رفع مشکل استفاده از حداقل ضایعات (min_waste)

## ❌ مشکل قبلی:

در نسخه قبلی، وقتی سیستم تکه‌های باقی‌مانده از برش را به انبار برمی‌گرداند، از **یک مقدار ثابت 70 سانتی‌متر** استفاده می‌کرد:

```python
if remaining > 70:  # همیشه 70 سانتی‌متر ثابت!
    if add_inventory_piece(profile_id, remaining, ...):
        added_pieces += 1
```

این یعنی:
- ❌ تنظیمات `min_waste` هر پروفیل نادیده گرفته می‌شد
- ❌ برای همه پروفیل‌ها، فقط تکه‌های بیشتر از 70cm برمی‌گشت
- ❌ اگر کاربر برای یک پروفیل 60cm یا 80cm تعریف کرده بود، تأثیری نداشت

---

## ✅ راه‌حل پیاده‌سازی شده:

حالا سیستم برای **هر پروفیل** مقدار `min_waste` را از دیتابیس می‌خواند و استفاده می‌کند:

```python
# خواندن min_waste از تنظیمات پروفیل
for p in profiles:
    if p['name'] == profile_name:
        profile_id = p['id']
        min_waste_threshold = float(p.get('min_waste', 70))
        break

# استفاده از min_waste پروفیل
for bin_data in bins_data:
    remaining = bin_data.get('remaining', 0)
    if remaining > min_waste_threshold:  # استفاده از تنظیمات پروفیل
        add_inventory_piece(profile_id, remaining, ...)
        added_pieces += 1
    elif remaining > 0:
        discarded_pieces += 1
```

---

## 🎯 مثال کاربردی:

### پروفیل A: min_waste = 60cm
- شاخه 1: باقی‌مانده 75cm → ✅ **برمی‌گردد** به انبار (75 > 60)
- شاخه 2: باقی‌مانده 50cm → ❌ **پرت می‌شود** (50 < 60)

### پروفیل B: min_waste = 80cm  
- شاخه 3: باقی‌مانده 90cm → ✅ **برمی‌گردد** به انبار (90 > 80)
- شاخه 4: باقی‌مانده 70cm → ❌ **پرت می‌شود** (70 < 80)

---

## 📊 پیام موفقیت بهبود یافته:

حالا پیام موفقیت شامل اطلاعات بیشتری است:

```
✓ فریم لس آلومینیومی: 5 شاخه کسر شد، 3 تکه (>60cm) به انبار برگشت، 2 تکه پرت شد
```

این پیام نشان می‌دهد:
- 5 شاخه از انبار کسر شد
- 3 تکه بزرگتر از 60cm به انبار برگشت
- 2 تکه کوچکتر از 60cm پرت شد

---

## ⚙️ مکان تنظیم min_waste:

کاربر می‌تواند در **مدیریت انبار → انواع پروفیل → ویرایش پروفیل** برای هر پروفیل مقدار `min_waste` را تعیین کند:

- مثلاً برای پروفیل‌های گران‌قیمت: 50cm (تا تکه‌های کوچکتر هم نگه داشته شود)
- مثلاً برای پروفیل‌های ارزان: 100cm (فقط تکه‌های بزرگ نگه داشته شود)

---

## 🔍 فایل‌های تغییر یافته:

- **cutting_web_app.py** - تابع `apply_cutting_plan` (خطوط 1071-1123)

---

## ✨ مزایای این تغییر:

1. ✅ **انعطاف‌پذیری بیشتر**: هر پروفیل تنظیمات خودش را دارد
2. ✅ **مدیریت بهتر انبار**: کاربر تصمیم می‌گیرد چه اندازه‌ای پرت است
3. ✅ **اطلاعات دقیق‌تر**: پیام موفقیت مشخص می‌کند چند تکه برگشت و چند تکه پرت شد
4. ✅ **سازگار با تنظیمات**: از تنظیمات تعریف‌شده در مدیریت انبار پیروی می‌کند

---

## 📅 تاریخ پیاده‌سازی:

28 دسامبر 2025 - نسخه بهبود یافته

